<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.huang.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".lyrics.LyricActivity" />
        <activity android:name=".process.ProcessActivity" />
        <activity android:name=".miTime.MiTimeActivity" />
        <activity android:name=".baiduProgress.BaiduProgressActivity" />
        <activity android:name=".gaussianBlur.GaussianBlurActivity" />
        <activity android:name=".fallObject.FallingActivity" />
        <activity android:name=".netHotClock.NetHotClockActivity" />
    </application>


</manifest>